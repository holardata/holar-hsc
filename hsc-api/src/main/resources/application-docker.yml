# 本文件基于 openCallHub（GPL-3.0）修改：修改者 jackzhang，2026-09
# Modified from openCallHub (GPL-3.0) by jackzhang, 2026-09.
# Docker 环境专用配置，覆盖 application.yml 中的本地地址
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/holarSmartCall?createDatabaseIfNotExist=true&useSSL=false&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true&useServerPrepStmts=true&cachePrepStmts=true
    username: root
    password: holardata2019
  data:
    redis:
      host: redis
      port: 6379
