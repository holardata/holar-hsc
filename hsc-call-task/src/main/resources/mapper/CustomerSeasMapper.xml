<?xml version="1.0" encoding="UTF-8"?>
<!-- 本文件基于 openCallHub（GPL-3.0）修改：修改者 jackzhang，2026-09 -->
<!-- Modified from openCallHub (GPL-3.0) by jackzhang, 2026-09. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hsc.calltask.mapper.CustomerSeasMapper">

    <resultMap type="com.hsc.calltask.domain.vo.CustomerSeasVo" id="CustomerSeasMap">
        <result property="id" column="id" jdbcType="BIGINT"/>
        <result property="templateId" column="template_id" jdbcType="BIGINT"/>
        <result property="templateName" column="template_name" jdbcType="VARCHAR"/>
        <result property="customerInfo" column="customer_info" typeHandler="com.hsc.system.handler.JsonObjectTypeHandler"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="source" column="source" jdbcType="INTEGER"/>
        <result property="ownerId" column="owner_id" jdbcType="BIGINT"/>
        <result property="ownerTime" column="owner_time" jdbcType="TIMESTAMP"/>
        <result property="lastDialTime" column="last_dial_time" jdbcType="TIMESTAMP"/>
        <result property="lastDialResult" column="last_dial_result" jdbcType="INTEGER"/>
        <result property="dialCount" column="dial_count" jdbcType="INTEGER"/>
        <result property="createBy" column="create_by" jdbcType="BIGINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateBy" column="update_by" jdbcType="BIGINT"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

  <sql id="selectCustomerSeasInfoVo">
     
   </sql>
    <select id="getDetail" resultMap="CustomerSeasMap">
        select cs.id,
               cs.template_id,
               ct.name as template_name,
               cs.customer_info,
               cs.phone,
               cs.name,
               cs.source,
               cs.owner_id,
               cs.owner_time,
               cs.last_dial_time,
               cs.last_dial_result,
               cs.dial_count,
               cs.create_by,
               cs.create_time,
               cs.update_by,
               cs.update_time,
               cs.del_flag
        from customer_seas cs
                 left join customer_template ct on cs.template_id = ct.id and ct.del_flag = 0
        <where>
            cs.id = #{id}
        </where>
    </select>


    <select id="getList" resultMap="CustomerSeasMap">
        select cs.id,
        cs.template_id,
        ct.name as template_name,
        cs.customer_info,
        cs.phone,
        cs.name,
        cs.source,
        cs.owner_id,
        cs.owner_time,
        cs.last_dial_time,
        cs.last_dial_result,
        cs.dial_count,
        cs.create_by,
        cs.create_time,
        cs.update_by,
        cs.update_time,
        cs.del_flag
        from customer_seas cs
        left join customer_template ct on cs.template_id = ct.id and ct.del_flag = 0
        <where>
            cs.del_flag = 0
            <if test="id != null">
                and cs.id = #{id}
            </if>
            <if test="phone != null and phone != ''">
                and cs.phone like concat('%',#{phone},'%')
            </if>
            <if test="name != null and name != ''">
                and cs.name like concat('%',#{name},'%')
            </if>
            <if test="source != null">
                and cs.source = #{source}
            </if>
            <if test="onlyPublic != null and onlyPublic">
                and cs.owner_id is null
            </if>
            <if test="ownerId != null">
                and cs.owner_id = #{ownerId}
            </if>
            <if test="templateIds != null and templateIds.size() > 0">
                and cs.template_id in
                <foreach item="item" index="index" collection="templateIds" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>

            <if test="beginTime != null">
                and cs.create_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and cs.create_time &lt;= #{endTime}
            </if>
        </where>
        order by cs.id desc
    </select>

</mapper>

